{
  "name": "server",
  "version": "1.0.0",
  "description": "production ready server",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "nodemon --env-file=.env index.js",
    "server:dev": "nodemon --env-file=.env index.js",
    "server:prod": "NODE_ENV=production node --env-file=.env index.js",
    "server:prod:mac": "NODE_ENV=production node --env-file=.env index.js"
  },
  "author": "Me",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.0",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "cryptr": "^6.0.3",
    "express": "^4.17.1",
    "mongodb": "^6.6.2",
    "socket.io": "^4.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
